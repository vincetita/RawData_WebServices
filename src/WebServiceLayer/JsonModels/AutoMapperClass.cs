using AutoMapper;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using DomainModel;


namespace WebServiceLayer.JsonModels
{
    public static class AutoMapperClass 
    {
        
        //public AutoMapperClass()
        //{
            
        //}
        //protected override void Configure()
        //{
        //    CreateMapper<Comments, CommentsModel>()
        //        .ForMember(dest => dest.CommentId, opt => opt.MapFrom(src => src.Url));
        //    CreateMapper<CommentsModel, Comments>()
        //        .ForMember(dest => dest.Url, opt => opt.MapFrom(src => src.CommentId));

        //    base.Configure();
        //}
    }
}
